

<h5>Atualizar Estoque</h5>

<%= form_with(url: '/search', method: 'get', local: true) do %>
    <div class="row justify-content-left padding">
        <div class="col-md-10">
            <form action="#" class="domain-form">
                <div class="form-group d-md-flex"> 
                    <%= select_tag :q,
                        options_for_select(@sesuppliers), class:"custom-select px-4", 
                        include_blank: "Selecione um Fornecedor.... " %>
                    
                    <%= submit_tag " Pesquisar Produto(s) ", class:"btn btn-primary px-5" %> 
                </div>
            </form>
        </div>
    </div>
<% end %>

<%= link_to 'Sair', homes_path, class: 'btn btn-dark btn-sm' %>
